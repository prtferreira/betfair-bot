server.port=8089
spring.profiles.active=${SPRING_PROFILES_ACTIVE:h2}

# Set these in your environment; do not hardcode secrets here.
betfair.app-key=jTNwpPVU6eIPUf9n
# betfair.session-token=

# Optional login-based flow (username/password). Prefer env vars instead of files.
betfair.username=adrianarita232@gmail.com
betfair.password=Pf62976297!
# betfair.auth.base-url=https://identitysso.betfair.com/api

logging.level.com.betfair.sim.service.BetfairApiClient=DEBUG
logging.level.com.betfair.sim.service.StatpalLiveClient=DEBUG
logging.level.com.betfair.sim.service.SimulationBetService=DEBUG

# DOM score scraping is disabled in backend.

# Local folder for followed match ID files saved from the UI submit action.
betfair.followed-games.dir=backend/data
# Legacy inPlayGames.txt monitor disabled; snapshots are written by SelectedGamesOddsCaptureService.
betfair.followed-games.monitor.enabled=false

# Simulation balances
betfair.simulation.start-balance=1000

# Local DB for recent_events stats cache.
spring.datasource.url=jdbc:h2:file:./backend/data/recent-events-db;MODE=PostgreSQL;AUTO_SERVER=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
